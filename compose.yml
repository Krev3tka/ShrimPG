services:
  shrimp_db:
    image: postgres:15-alpine
    container_name: shrimp_db
    environment:
      - POSTGRES_USER=shrimp_user
      - POSTGRES_PASSWORD=shrimp_password
      - POSTGRES_DB=shrimp_vault
    restart: unless-stopped
    ports:
        - "5433:5432"